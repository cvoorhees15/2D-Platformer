using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class SoundManager : MonoBehaviour
{

    public static AudioClip enemyIsHitSound, playerWalkSound, playerIsHitSound, gameMusicSound;
    static AudioSource audioSrc;

    // Start is called before the first frame update
    void Start()
    {
        enemyIsHitSound = Resources.Load<AudioClip>("HammerFlesh1");
        playerWalkSound = Resources.Load<AudioClip>("Walking in ChainMail - Loop");
        playerIsHitSound = Resources.Load<AudioClip>("Battleaxe5");
        gameMusicSound = Resources.Load<AudioClip>("05cave3");

        audioSrc = GetComponent<AudioSource>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public static void PlaySound (string clip)
    {
        switch (clip)
        {
            case "enemyHit":
                audioSrc.PlayOneShot(enemyIsHitSound);
                break;

            case "playerHit":
                audioSrc.PlayOneShot(playerIsHitSound);
                break;



        }
    }
}*/
